import React from 'react'

const NameArr = ({ arr, isNav = false }) => {
    return (
        <div>
            <div>this will be the bar next to the selected </div>
            <div>arr</div>
        </div>
    );
}

export default NameArr;